from sys import stdin

cases = list(map(lambda x: [int(i) for i in x], map(lambda x: x.split(), stdin.readlines()[:-1])))

for case in cases:
    cards = [i for i in range(1,53) if i not in case]
    prince, sis = 0, 0
    prince += 1 if case.pop(max(case[:3])) < case.pop(min(case[2:])) else sis += 1
    try:
        print(cards[0])
    except IndexError:
        print(-1)
